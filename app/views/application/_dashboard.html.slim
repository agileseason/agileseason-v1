.header
  .logo
    span Agile season

  .user
    .avatar style="background-image: url(#{raw gravatar_image_url(current_user.email, size: 60)})"
    .name
      span #{current_user}&nbsp;&mdash;&nbsp;
      a.sign-out href="#{sign_out_url}" sign out

.board-lists
  - if params[:controller] == 'boards' && params[:action] == 'index'
    = render partial: 'board_list'

  - else
    .b-preloader.blue-medium

.b-wizard
  ul.menu
    li.active Choose repository
    span.arrow &nbsp;→&nbsp;
    li Set up board

  ul.repos-list
    .b-preloader.blue-medium

  .new-board-form
    .b-preloader.blue-medium
