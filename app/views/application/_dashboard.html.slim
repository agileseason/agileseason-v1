.header
  .logo
    span Agile season

  .user
    img.avatar src="#{github_avatar_url(current_user)}"
    .name
      span #{current_user}&nbsp;&mdash;&nbsp;
      a.sign-out href="#{sign_out_url}" sign out

.board-lists
  - if controller_name == 'boards' && action_name == 'index'
    = render partial: 'board_list'

  - else
    .b-preloader.blue-medium

.b-wizard
  ul.menu
    li.active Choose repository
    span.arrow &nbsp;→&nbsp;
    li Set up board

  ul class="#{RepoList.to_css}"
    .b-preloader.blue-medium

  .new-board-form
    .b-preloader.blue-medium
