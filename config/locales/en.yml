en:
  activerecord:
    models:
    errors:
      models:
        board:
          attributes:
            columns:
              too_short:
                few: is to few columns (minimum is %{count})
                many: is to few columns (minimum is %{count})
                one: is to few columns (minimum is %{count})
                other: is to few columns (minimum is %{count})
